create table locations (
    id bigserial primary key,
    site_id int not null,
    description text not null,
    coordinate point not null,
    unique (_id)
);

create index _id_index on locations (_id);
